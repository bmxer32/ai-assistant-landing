/*
 * Конфиг живого чата (Gemini).
 *
 * 1. Скопируйте этот файл как config.js (он в .gitignore — в git не попадёт).
 * 2. Получите бесплатный ключ: https://aistudio.google.com/apikey
 * 3. Вставьте его в geminiKey ниже.
 *
 * Без ключа чат работает в режиме витрины: отвечает готовыми репликами.
 * ВАЖНО: ключ в браузере виден любому посетителю. Для публичного сайта
 * нужен серверный прокси — не выкладывайте config.js в интернет.
 */
window.SITE_CONFIG = {
  geminiKey: "",
  geminiModel: "gemini-2.0-flash"
};
